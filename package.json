{
  "name": "timeline-proyectos",
  "version": "1.0.0",
  "description": "Sistema de gestión municipal - Alcaldía de Copacabana",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "vercel dev",
    "test": "node test/test-api.js",
    "test:local": "npm run test -- --local",
    "build": "echo 'No build step required'",
    "deploy:vercel": "vercel --prod",
    "deploy:netlify": "netlify deploy --prod"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^3.3.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vercel": "^32.0.0",
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
